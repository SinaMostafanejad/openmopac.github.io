.. _LINMIN:

``LINMIN``
==========

There are two line-minimization routines in MOPAC, an energy
minimization (if ``BFGS`` is used) and a gradient norm minimization (if
``SIGMA`` is used). ``LINMIN`` will output details of the line
minimization used in a given job.
