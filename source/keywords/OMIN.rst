.. _OMIN:

``OMIN=n.nn``
=============

In a ``TS`` calculation, the minimum allowable overlap of the transition
state eigenvectors is set by *n*.\ *nn*. If ``OMIN`` is *not* set, the
default of 0.8 is used.
