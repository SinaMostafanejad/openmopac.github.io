.. _TIMES:

``TIMES``
=========

The times for various stages of the calculation are printed when
``TIMES`` is specified. This is very useful in finding out what is using
a lot of time. A second use is to find out precisely where in a run a
failure occurred.
