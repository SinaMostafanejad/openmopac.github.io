.. _DDMIN:

``DDMIN``
=========

The minimum size of the trust radius in ``EF`` and ``TS`` is set to
*n*.\ *nn*. If ``DDMIN`` is *not* set, the default of 0.001 is used. Set
``DDMIN`` to a small number, e.g. 0.00001, if the starting geometry is
very near to the optimized geometry.
